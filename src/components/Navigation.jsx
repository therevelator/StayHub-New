import React from 'react';
import { Link } from 'react-router-dom';

function Navigation() {
  // Make sure there's no duplicate header elements here
  return (
    <nav>
      {/* Navigation content */}
    </nav>
  );
}

export default Navigation; 
// Remove any import of Header or navigation components
// import Header from '../components/Header'; // Remove this if it exists
// import Navigation from '../components/Navigation'; // Remove this if it exists

function Planning() {
  // Make sure there's no <Header /> component here
  return (
    <div>
      {/* Planning page content only, no header */}
    </div>
  );
}

export default Planning; 
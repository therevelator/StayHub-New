# The Express server serves the built client same-origin, so the API is always
# reachable at a relative /api — both on the deployed host and when the local
# server serves this production build. Never bake an absolute host here.
VITE_API_URL=/api

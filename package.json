{
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm start --prefix server",
    "start": "concurrently \"npm run client\" \"npm run server\"",
    "start:ci": "concurrently \"npm run dev --prefix client -- --host 127.0.0.1 --port 3000 --strictPort\" \"npm start --prefix server\"",
    "test:e2e": "cypress run",
    "cypress:run": "cypress run",
    "cypress:open": "cypress open",
    "build:railway": "npm ci --prefix server && npm ci --include=dev --prefix client && npm run build --prefix client",
    "start:railway": "npm start --prefix server"
  },
  "dependencies": {
    "@react-google-maps/api": "^2.20.3",
    "jspdf": "^3.0.1",
    "jspdf-autotable": "^5.0.2",
    "opencage-api-client": "^1.0.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-helmet": "^6.1.0",
    "react-helmet-async": "^2.0.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cypress": "^13.17.0",
    "dotenv": "^17.4.2",
    "mysql2": "^3.23.1"
  }
}
